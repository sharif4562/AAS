```python id="g4t2hx"
# SESSION 27 – AMAZON (Go-To-Goal)
# 2D Navigation – Distance Calculation Example

# Example robot and goal positions
robot_pos = (2, 3)   # x, y
goal_pos = (8, 7)    # x, y

# Euclidean distance
distance = ((goal_pos[0] - robot_pos[0])**2 + (goal_pos[1] - robot_pos[1])**2)**0.5

print("Distance =", round(distance, 1), "m")
```

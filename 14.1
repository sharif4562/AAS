```python id="kq4n8v"
# SESSION 14 – NVIDIA (Visual SLAM)
# TUM RGB-D – Pose Update

# Example previous pose (x, y)
prev_pose = (1.0, 1.5)

# Example motion delta (dx, dy)
motion = (1.4, 2.1)

# Update pose
new_pose = (prev_pose[0] + motion[0], prev_pose[1] + motion[1])

print(new_pose)
```

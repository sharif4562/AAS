```python id="f7k2ld"
# SESSION 22 – AMAZON (Kinematics)
# Differential Drive – Straight Motion Check

# Example wheel velocities (left and right wheels)
left_wheel_velocity = 1.0   # m/s
right_wheel_velocity = 1.0  # m/s

# Check motion type
if left_wheel_velocity == right_wheel_velocity:
    motion_type = "Straight Motion"
else:
    motion_type = "Turning Motion"

print(motion_type)
```

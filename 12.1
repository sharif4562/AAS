```python id="wz9fut"
# SESSION 12 – GOOGLE (Place Recognition)
# Oxford RobotCar – Similarity Check for Place Recognition

# Example similarity score from feature matching
similarity = 0.92

# Rule-based recognition
if similarity > 0.9:
    recognized = True
else:
    recognized = False

print("Place Recognized" if recognized else "Place Not Recognized")
```
